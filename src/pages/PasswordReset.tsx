import React from "react";
import { UserLayout } from "@/components/UserLayout";

const PasswordReset = () => (
  <UserLayout title="Password Reset">
    <div>This is the Password Reset page.</div>
  </UserLayout>
);

export default PasswordReset; 